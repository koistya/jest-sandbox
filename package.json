{
  "name": "jest-sandbox",
  "private": true,
  "engines": {
    "node": ">=0.12.0",
    "npm": ">=2.5.0"
  },
  "dependencies": {
    "react": "^0.12.2"
  },
  "devDependencies": {
    "babel-core": "^4.6.0",
    "jest-cli": "^0.4.0"
  },
  "jest": {
    "rootDir": "src",
    "scriptPreprocessor": "../preprocessor.js",
    "unmockedModulePathPatterns": [
      "\\.json$",
      "node_modules(/|\\\\)react"
    ]
  },
  "scripts": {
    "test": "jest"
  }
}
